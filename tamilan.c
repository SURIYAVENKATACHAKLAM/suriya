#include<stdio.h>
int main()
{
    int num,n;
    printf("enter the number");
    scanf("%d",&num);
    while(number%10!=0)
    {
        numb++;
    }
printf("nearest greater multiple of 10 %d",num);
    }
    return 0;
